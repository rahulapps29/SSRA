import React from "react";
import ProductGallery from "./ProductGallery"; // Reuse the existing gallery component
import ServicesSection from "./ServicesSection";
const GalleryPage = () => {
  return (
    <div style={{ padding: "5px" }}>
    
      {/* <ServicesSection /> */}
    </div>
  );
};

export default GalleryPage;
